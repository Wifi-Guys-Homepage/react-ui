import React from 'react'
const CV = () => {
    return (
        <div>
            Download link to Default CV, ability to generate CV normally or via ML - ML will have uuid to get it from S3
        </div>
    );
};export default CV;