import React from 'react'
const Appinfo = () => {
    return (
        <div>
            Appinfo page including all APIs + DB, also does healthcheck on each one
        </div>
    );
};export default Appinfo;